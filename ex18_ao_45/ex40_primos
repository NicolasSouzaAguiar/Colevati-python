a = int(input("digite o primeiro número: "))
b = int(input("igite o segundo número: "))

if a > b:
    inicio = b
    fim = a
else:
    inicio = a
    fim = b

for n in range(inicio, fim + 1):
    if n > 1:
        primo = True

        for i in range(2, n):
            if n % i == 0:
                primo = False
                break

        if primo:
            print(n)